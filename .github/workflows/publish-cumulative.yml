name: Publish cumulative exposure - CORRECTED

on:
  schedule:
    - cron: '30 3 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup R
        uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true

      - name: System libraries
        run: |
          sudo apt-get update
          sudo apt-get install -y libgdal-dev libgeos-dev libproj-dev libudunits2-dev jq curl

      - name: Get R version
        id: r-version
        run: echo "version=$(R --version | head -n1 | awk '{print $3}')" >> $GITHUB_OUTPUT

      - name: Cache R packages
        uses: actions/cache@v3
        with:
          path: ${{ env.R_LIBS_USER }}
          key: ${{ runner.os }}-r-${{ steps.r-version.outputs.version }}-packages-v3
          restore-keys: ${{ runner.os }}-r-${{ steps.r-version.outputs.version }}-packages-

      - name: Install R packages
        run: |
          Rscript -e 'pkgs <- c("sf","dplyr","fs","purrr","jsonlite","data.table"); inst <- setdiff(pkgs, rownames(installed.packages())); if(length(inst)) { cat("Installing", length(inst), "packages\n"); install.packages(inst, repos="https://cloud.r-project.org", Ncpus=2) } else { cat("All packages cached\n") }'

      - name: Restore data cache
        uses: actions/cache@v3
        with:
          path: data/daily/
          key: hq-daily-data-${{ github.run_id }}
          restore-keys: hq-daily-data-

      - name: Restore hex grid cache
        uses: actions/cache@v3
        with:
          path: public/hex_grid_template.rds
          key: hq-hex-grid-v1
          restore-keys: hq-hex-grid-

      # ============================================================
      # FIX 1: Get fresh access token from refresh token
      # ============================================================
      - name: Get Fresh Dropbox Access Token
        id: get_token
        env:
          DROPBOX_REFRESH_TOKEN: ${{ secrets.DROPBOX_REFRESH_TOKEN }}
          DROPBOX_APP_KEY: ${{ secrets.DROPBOX_APP_KEY }}
          DROPBOX_APP_SECRET: ${{ secrets.DROPBOX_APP_SECRET }}
        run: |
          echo "🔑 Getting fresh access token from refresh token..."
          
          RESPONSE=$(curl -s -X POST https://api.dropbox.com/oauth2/token \
            -u "${DROPBOX_APP_KEY}:${DROPBOX_APP_SECRET}" \
            -d "grant_type=refresh_token&refresh_token=${DROPBOX_REFRESH_TOKEN}")
          
          ACCESS_TOKEN=$(echo "$RESPONSE" | jq -r '.access_token // empty')
          
          if [ -z "$ACCESS_TOKEN" ]; then
            echo "❌ Failed to get access token. Response:"
            echo "$RESPONSE" | jq '.'
            exit 1
          fi
          
          echo "✅ Successfully obtained fresh access token"
          echo "access_token=$ACCESS_TOKEN" >> $GITHUB_OUTPUT

      # ============================================================
      # Download files using FRESH token with pagination
      # ============================================================
      - name: Download files from Dropbox (with pagination)
        env:
          DROPBOX_TOKEN: ${{ steps.get_token.outputs.access_token }}
        run: |
          set -eo pipefail
          
          echo "📥 Listing Dropbox files (with pagination support)..."
          
          ALL_FILES=""
          HAS_MORE=true
          CURSOR=""
          PAGE=1
          
          while [ "$HAS_MORE" = "true" ]; do
            echo "  Fetching page $PAGE..."
            
            if [ -z "$CURSOR" ]; then
              RESPONSE=$(curl -s -X POST https://api.dropboxapi.com/2/files/list_folder \
                -H "Authorization: Bearer $DROPBOX_TOKEN" \
                -H "Content-Type: application/json" \
                -d '{"path": "/hq-outages", "recursive": true}')
            else
              RESPONSE=$(curl -s -X POST https://api.dropboxapi.com/2/files/list_folder/continue \
                -H "Authorization: Bearer $DROPBOX_TOKEN" \
                -H "Content-Type: application/json" \
                -d "{\"cursor\": \"$CURSOR\"}")
            fi
            
            if echo "$RESPONSE" | jq -e '.error' > /dev/null 2>&1; then
              echo "❌ Error from Dropbox API:"
              echo "$RESPONSE" | jq '.error'
              exit 1
            fi
            
            if ! echo "$RESPONSE" | jq -e '.entries' > /dev/null 2>&1; then
              echo "⚠️  Unexpected response - continuing with empty list"
              break
            fi
            
            PAGE_COUNT=$(echo "$RESPONSE" | jq '.entries | length')
            echo "    Found $PAGE_COUNT entries"
            
            PAGE_FILES=$(echo "$RESPONSE" | jq -r '.entries[]? | select(.".tag" == "file") | .path_display' || true)
            
            if [ ! -z "$PAGE_FILES" ]; then
              if [ -z "$ALL_FILES" ]; then
                ALL_FILES="$PAGE_FILES"
              else
                ALL_FILES="$ALL_FILES"$'\n'"$PAGE_FILES"
              fi
            fi
            
            HAS_MORE=$(echo "$RESPONSE" | jq -r '.has_more')
            
            if [ "$HAS_MORE" = "true" ]; then
              CURSOR=$(echo "$RESPONSE" | jq -r '.cursor')
              PAGE=$((PAGE + 1))
            fi
          done
          
          echo "✅ Completed pagination: $PAGE page(s)"
          
          TOTAL_COUNT=0
          if [ ! -z "$ALL_FILES" ]; then
            TOTAL_COUNT=$(echo "$ALL_FILES" | wc -l)
          fi
          echo "📊 Total files: $TOTAL_COUNT"
          
          if [ $TOTAL_COUNT -eq 0 ]; then
            echo "⚠️  No files found - continuing with cached data"
            mkdir -p data/daily
            exit 0
          fi
          
          POLYGONS=$(echo "$ALL_FILES" | grep "/polygons_.*\.geojson$" || true)
          POLY_COUNT=$(echo "$POLYGONS" | wc -l 2>/dev/null || echo "0")
          
          echo "📍 Polygon files: $POLY_COUNT"
          
          if [ "$POLY_COUNT" -eq 0 ]; then
            echo "⚠️  No polygon files - continuing with cached data"
            mkdir -p data/daily
            exit 0
          fi
          
          mkdir -p data/daily
          
          CACHED=0
          NEW=0
          
          while IFS= read -r file; do
            [ -z "$file" ] && continue
            
            filename=$(basename "$file")
            local_path="data/daily/$filename"
            
            if [ -f "$local_path" ] && [ -s "$local_path" ]; then
              CACHED=$((CACHED + 1))
            else
              if curl -s -f -X POST https://content.dropboxapi.com/2/files/download \
                -H "Authorization: Bearer $DROPBOX_TOKEN" \
                -H "Dropbox-API-Arg: {\"path\": \"$file\"}" \
                -o "$local_path" && [ -s "$local_path" ]; then
                NEW=$((NEW + 1))
                [ $NEW -le 5 ] && echo "  ✓ Downloaded: $filename"
              fi
            fi
          done <<< "$POLYGONS"
          
          echo ""
          echo "📊 Summary: $CACHED cached, $NEW new, $(($CACHED + $NEW)) total"

      - name: Run R analysis
        run: |
          echo "🔬 Starting analysis..."
          
          if [ ! -f "R/build_cumulative_hex.R" ]; then
            echo "❌ Script not found!"
            exit 1
          fi
          
          FILE_COUNT=$(find data/daily -name "polygons_*.geojson" 2>/dev/null | wc -l)
          echo "📊 Processing $FILE_COUNT files"
          
          if [ "$FILE_COUNT" -eq 0 ]; then
            echo "❌ No data to process!"
            exit 1
          fi
          
          Rscript R/build_cumulative_hex.R

      - name: Verify outputs were created
        run: |
          echo "📁 Verifying generated files..."
          
          if [ ! -f "public/index.html" ]; then
            echo "❌ index.html not created!"
            exit 1
          fi
          
          if [ ! -f "public/total/total_exposure.geojson" ]; then
            echo "❌ total_exposure.geojson not created!"
            exit 1
          fi
          
          DAILY_COUNT=$(find public/daily -name "daily_*.geojson" 2>/dev/null | wc -l)
          echo "✅ Generated $DAILY_COUNT daily summary files"
          
          if [ "$DAILY_COUNT" -eq 0 ]; then
            echo "⚠️  Warning: No daily summaries created"
          fi
          
          echo ""
          echo "📊 Public directory structure:"
          du -sh public/*/
          
          echo ""
          echo "✅ All required files present"

      # ============================================================
      # FIX 2: Deploy to EXTERNAL PUBLIC repo (not same repo)
      # ============================================================
      - name: Deploy to PUBLIC repo (gh-pages)
        uses: peaceiris/actions-gh-pages@v3
        with:
          personal_token: ${{ secrets.PUBLIC_REPO_TOKEN }}
          external_repository: evelynebrie/hq-outages-public
          publish_branch: gh-pages
          publish_dir: public
          keep_files: false
          commit_message: "Update cumulative outage data - ${{ github.run_number }}"
          
      - name: Summary
        run: |
          echo ""
          echo "========================================="
          echo "✅ DEPLOYMENT COMPLETE"
          echo "========================================="
          echo ""
          echo "🌐 View at: https://evelynebrie.github.io/hq-outages-public/"
          echo ""
          DAILY_COUNT=$(find public/daily -name "daily_*.geojson" 2>/dev/null | wc -l)
          echo "📊 Deployed $DAILY_COUNT daily summaries"
          echo "========================================="
